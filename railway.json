{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd mbo_youth_emp && pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate"
  },
  "deploy": {
    "startCommand": "cd mbo_youth_emp && gunicorn config.wsgi"
  }
}
